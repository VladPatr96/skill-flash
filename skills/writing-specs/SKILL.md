---
name: writing-specs
description: Дисциплина написания самодостаточной спеки в тело issue — исполнитель работает без разведки и вопросов. Use WHENEVER writing or rewriting an issue body before dispatch (skill-flash issue-first pipeline), or when an executor asked a question / did unplanned exploration (symptom of a weak spec).
---

# Writing-specs: спека, снимающая мышление

Спека — единственный документ задачи. Исполнитель должен сделать работу, **не открыв ни одного файла «на разведку» и не задав ни одного вопроса**. Не прошло этот тест — спека не готова, диспатч запрещён.

## Структура (тело issue переписывается целиком: `gh issue edit N --body`)

1. **Цель** — одно предложение: что видит пользователь после мержа. Исходная формулировка задачи растворяется здесь.
2. **Контекст** — файлы и строки, контракты, ловушки (дубли, генерённое). Источник — `graphify query` и отчёты скаутов, не память. Для weak-исполнителей — вырезки кода прямо сюда (см. дисциплину weak-dispatch).
3. **Влияние** — кто зависит от изменяемого (`graphify query "what depends on X"`), что может сломаться. Без этой секции правка не диспатчится.
4. **Решённые развилки** — каждая: решение + обоснование одной строкой. Развилки решает автор спеки, не исполнитель. Меняет скоуп/деньги — стоп, вопрос фаундеру (архитектурная — консилиум).
5. **Шаги** — нумерованный план правок по файлам; для кода шаг 1 — тест, фиксирующий требование (red → green).
6. **Границы** — что НЕ делать: не рефакторить попутно, не трогать генерённое, не выходить за ownership отдела.
7. **DoD + проверка** — чек-лист приёмки + конкретная команда/сценарий (что запустить, что должно быть видно). Меняется поведение → пункт «обновить доки»; кодовая задача → пункт «graphify update . прогнан».

## Чек-лист выхода

- [ ] Тест самодостаточности: можно исполнить, не открыв ничего сверх спеки?
- [ ] Все развилки решены и записаны (ни одного «на усмотрение исполнителя»)?
- [ ] DoD исполним одной командой/сценарием (verifier не должен ничего изобретать)?
- [ ] Дифф-бюджет реалистичен (для weak: один файл, ≤40 строк — иначе дробить)?
- [ ] Лейбл `status:spec-ready` поставлен?

Симптом на будущее: исполнитель задал вопрос или полез исследовать → это дефект СПЕКИ, не исполнителя. На ретро — правка этой дисциплины.
